nextflowVersion = '>=20.10.0'

trace.enabled = true
report.enabled = true
dag.enabled = true

singularity.enabled = true
singularity.autoMounts = true
singularity.pullTimeout = '1h'

process {
    executor = 'slurm'
    containerOptions = '--bind "/scratch:/scratch" --bind "/nfs:/nfs" --bind "/gpfs:/gpfs" --bind "/home:/home"'
    clusterOptions = '--account=scjp1'
    beforeScript = 'module load singularity/4.4.1'
    shell = ['/bin/bash', '-ueo', 'pipefail']
}

executor {
    name = 'local'
    cpus = 5
    memory = '32 GB'
}

process.container = 'docker://ncarrut/singlecell_qc:second'

params {
    sample_id = null
    rna_results_dir = null
    atac_results_dir = null
    outdir = "results"
    filter_MT_ATAC = false
}
